Q85: Reverse a string.

/*
Sample Test Cases:
Input 1:
abcd
Output 1:
dcba

*/

//Solution:
#include <stdio.h>

int main() {
    char str[100];
    int len = 0, i;
    gets(str);
    while (str[len] != '\0')
        len++;
    for (i = len - 1; i >= 0; i--)
        printf("%c", str[i]);
    printf("\n");
    return 0;
}

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Q86: Check if a string is a palindrome.

/*
Sample Test Cases:
Input 1:
madam
Output 1:
Palindrome

Input 2:
hello
Output 2:
Not palindrome

*/

//Solution:
#include <stdio.h>

int main() {
    char str[100];
    int i, len = 0, flag = 0;
    gets(str);
    while (str[len] != '\0')
        len++;
    for (i = 0; i < len / 2; i++) {
        if (str[i] != str[len - i - 1]) {
            flag = 1;
            break;
        }
    }
    if (flag == 0)
        printf("Palindrome\n");
    else
        printf("Not palindrome\n");
    return 0;
}
